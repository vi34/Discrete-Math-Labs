#include <iostream>
#include <set>
#include <vector>

using namespace std;

vector<tuple<int, int, int> > graph;

int main() {
    freopen("rainbow.in","r",stdin);
    freopen("rainbow.out","w",stdout);
    int n, m;
    cin >> n >> m;
    graph.resize(m);
    set<int> answ;
    bool isMax = false;
    while (!isMax) {

    }
    return 0;
}